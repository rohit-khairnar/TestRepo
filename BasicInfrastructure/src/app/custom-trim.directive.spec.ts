import { CustomTrimDirective } from './custom-trim.directive';

describe('CustomTrimDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTrimDirective();
    expect(directive).toBeTruthy();
  });
});
